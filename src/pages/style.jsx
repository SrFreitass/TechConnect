import { styled } from "styled-components";

export const MetaversoIframe = styled.iframe`
  width: 100%;
  height: 100vh;
  border: none;
`;
export const StyleFastPage = styled.div`
  @media (max-width: 500px) {
    header {
      display: none;
    }
  }
`;
